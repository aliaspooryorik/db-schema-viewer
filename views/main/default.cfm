<cfoutput>

<h1>Hello!</h1>

<p>This a little tool that attempts to work out the relationship between tables using naming conventions if the database has no foreign keys. You can alos serach for a column name to see which tables it exists in.</p>
<p>To get started, select a tablename or type in a column name</p>
</cfoutput>
